<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Shital Janvalkar</title>
</head>
<body>
    <h1>Weather Information</h1>
    <form action="/getWeather" method="POST">
        <label for="cities">Select cities:</label>
        <select name="cities" id="cities" multiple>
            <option selected="selected">Toronto</option>
            <option selected="selected">Mumbai</option>
            <option selected="selected">London</option>
            <!-- Add more cities as needed -->
        </select>
        <br>
        <button type="submit">Get Weather</button>
    </form>
    <br>
    <ul>
        <% for (const city in weather) { %>
            <li><%= city %>: <%= weather[city] %></li>
        <% } %>
    </ul>
</body>
</html>